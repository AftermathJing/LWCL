extends: ../../tuning/signal_v2_tuning_base.yaml

experiment:
  id: FSTC_ENCODER_NO_TEMPORAL
  description: Remove temporal stem, local/global temporal Transformers, and temporal position encoding

ablation:
  variant: no_temporal_encoder
